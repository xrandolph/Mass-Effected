# Godot 4.x generated/cache
.godot/

# Godot 3.x leftovers (harmless to ignore)
.import/
export.cfg
export_presets.cfg

# Import metadata files
*.import

# Mono / C# (if you ever enable it)
.mono/
*.mono

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Optional: conversion artifacts (safe to ignore if you don't rely on it)
.autoconverted/
